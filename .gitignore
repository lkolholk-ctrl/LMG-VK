# Android / Gradle
.gradle/
build/
*/build/
local.properties
*.iml
.idea/
.cxx/
.externalNativeBuild/

# media3-m2 поставляется CI/вручную (форк media3-lmg)
media3-m2/

# OS
.DS_Store

# Конфиг обхода блокировок: адреса и сертификаты. Репозиторий публичный —
# документ живёт только на сервере и отдаётся приложению в рантайме.
network-config.json
**/network-config.json

# Local dependencies used only to regenerate the embedded AMLL renderer.
tools/amll-renderer/node_modules/
